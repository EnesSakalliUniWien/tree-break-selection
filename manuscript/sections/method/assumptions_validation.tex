% !TEX root = ../../main.tex

\subsection{Assumptions, Tuning Parameters, and Validation Status}
\label{sec:assumptions-validation}

The preceding subsections define the implemented algorithm. This subsection
separates mathematical assumptions from implementation choices and records
which parts still require validation before the manuscript can support
submission-level claims.

\paragraph{Statistical assumptions.}
The edge and sibling standardizations use the covariance model declared by the
active feature-space block. Bernoulli blocks use Bernoulli variances, categorical
one-hot blocks use a drop-last multinomial covariance, and continuous blocks use
empirical-Gaussian covariance estimated from descendant leaves. These models are
exact only under their corresponding fixed-membership sampling assumptions and
become approximations for correlated features, high-cardinality one-hot blocks,
finite-sample continuous covariance estimates, discretized benchmark variants,
and data-dependent subtrees. The projected-Wald reference further treats the
selected orthonormal projection rows as fixed when evaluating the projected
quadratic form. This yields a chi-square reference for a fixed subspace under
an isotropic standardized null, but it is not by itself an exact selective
inference result for a hierarchy learned from the same data. Selective
inference after data-dependent hypothesis selection requires conditioning on
the selection event \citep{fithian2014optimal,lee2016exact}; for hierarchical
clustering, this distinction is explicit in selective tests for differences in
cluster means after agglomerative clustering \citep{gao2024selective}.

\paragraph{High-dimensional spectral assumptions.}
The Marchenko--Pastur rule is used as a local signal-dimension heuristic for
correlation-scale spectra. Random-matrix theory gives a useful reference for
noise eigenvalue bulk behavior in high-dimensional settings
\citep{marchenko1967distribution,johnstone2018pca}, but the implementation
applies the rule to local subtree matrices that include both leaf rows and
internal-node distribution rows. Those local matrices are structured,
overlapping summaries rather than independent observations from a classical
spiked covariance model. The MP threshold should therefore be presented as a
dimension-selection rule requiring finite-sample validation, not as a proven
optimal detector in this setting.

\paragraph{Empirical-null inflation assumptions.}
The sibling inflation model assumes that tree-selected sibling statistics
share a context-dependent multiplicative scale distortion. The empirical estimator uses
joint child-edge null-evidence weights, feature-family matching, and a local
context over log sibling projection dimension and, for non-Bernoulli feature
families, log parent size. These choices define a practical calibration model.
They do not estimate posterior null probabilities. The restored production
rule uses a plug-in chi-square p-value with empirical inflation when internal
support exists; its selected-tail validity remains unproven. The open mathematical object is the selected-hierarchy law for the
selected ratio, not merely a mean inflation factor. A separate exact-F path
applies only to mutually independent focal and calibration chi-square
statistics, fixed unit weights, a common scale, and disjoint observation
ownership.

\begingroup
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.23\linewidth}>{\raggedright\arraybackslash}p{0.21\linewidth}>{\raggedright\arraybackslash}p{0.19\linewidth}>{\raggedright\arraybackslash}p{0.31\linewidth}@{}}
\caption{Implemented method choices and unresolved calibration objects that are
not yet fully validated as manuscript-level scientific defaults.}
\label{tab:method-constants}\\
\toprule
Choice & Mathematical role & Current value & Validation status \\
\midrule
\endfirsthead
\toprule
Choice & Mathematical role & Current value & Validation status \\
\midrule
\endhead
$\alpha_{\mathrm{edge}}$ & Tree-BH edge testing level & $0.001$ & Unvalidated default; needs sensitivity over null and planted-signal regimes. \\
$\alpha_{\mathrm{sib}}$ & BH level for focal sibling pairs & $0.01$ & Unvalidated default; needs calibration and power trade-off study. \\
Marchenko--Pastur upper edge & Local projection-dimension rule & $(1+\sqrt{d_u/m_u})^2$ & Theoretically motivated for noise spectra; finite local subtree use is unvalidated. \\
Minimum spectral dimension & Projection-dimension floor & $2$ & Implementation regularizer; needs ablation against floors $0$, $1$, and $2$. \\
Spectral row set & Rows used to estimate the local PCA basis & descendant leaves only & Internal summary rows are excluded from the inferential PCA basis after selected-PCA validation showed anti-conservative calibration. \\
Sibling dimension rule & Transfers edge dimensions to sibling test & geometric mean of child edge dimensions & Needs sensitivity against parent dimension and min/max alternatives. \\
Empirical-null weight $\pi_q$ & Weights calibration records by joint child-edge null evidence & product of the two child-edge null-evidence weights & Monotone empirical weight; not a posterior probability; needs calibration check. \\
Context bandwidth $h$ & Localizes inflation estimates within feature family & null-weighted empirical standard deviation over active log-context axes & Needs sensitivity and behavior under few unique dimensions or parent sizes. \\
Barycentric leverage variables & Describe selected child balance and edge/sibling shared-direction geometry & $b_u$, $\ell_u^{\mathrm{bar}}$, and $s_u^{\mathrm{samp}}$ in selected-tail diagnostics & Diagnostic coordinates; the current Q5 rerun improves some tail-error metrics but does not validate a production external law. \\
Effective calibration sample size & Reports degeneracy of calibration dependency-group weights & diagnostic only & Nested records sharing one stopping event count once; thresholds still need validation. \\
Sibling calibration reference law & Determines whether a scale estimate yields a calibrated p-value & selected-hierarchy law unresolved; restricted independent common-scale exact-F mode available & The empirical plug-in tail is active with internal support, without a selected-tail guarantee; exact mode requires its full independence and ownership contract. \\
External selected-hierarchy law & Would replace unsupported internal empirical-null contexts & no general production path; narrow small-parent, high-edge-action selected-tail contexts are diagnostic-admissible for some feature families & Requires admissible support and selected-ratio tail validation in each target context before use. \\
Pass-through traversal & Allows deeper splits after shallow sibling failure & enabled & Algorithmic policy; needs ablation on over-split and under-split behavior. \\
\bottomrule
\end{longtable}
\endgroup

[VALIDATION GAP: each row of \Cref{tab:method-constants} requires either a
derivation, a null calibration study, a sensitivity analysis, or a justified
decision to label the value as an implementation default rather than a
statistical optimum.]
