\documentclass[11pt]{article}

\usepackage{iftex}
\ifPDFTeX
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\fi
\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage[numbers]{natbib}
\usepackage{float}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, backgrounds, calc, fit, matrix, positioning}
\usepackage{hyperref}
\usepackage[capitalise,nameinlink,noabbrev]{cleveref}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}

\title{Tree-Break Selection: Statistically Filtered Hierarchical Clustering with Projected-Wald Tests}
\author{Draft workspace}
\date{\today}

\begin{document}

\maketitle

\begin{abstract}
Hierarchical clustering exposes many candidate splits, but a geometric tree
cut does not say which splits are statistically credible. Tree-Break Selection
(TBS) is a
statistical filtering method for hierarchical clustering on typed
sample-feature data. The current implementation supports Bernoulli coordinates,
categorical one-hot blocks with a drop-last multinomial covariance model, and
continuous empirical-Gaussian blocks. It builds a candidate hierarchy,
summarizes each subtree by its empirical feature distribution, and annotates
candidate splits with two tests. The child-parent edge test asks whether a child
departs from its parent under the relevant feature-space covariance model. The
sibling test asks whether the two children under the same parent are distinct.
Context-weighted empirical-null records estimate selected-hierarchy scale
distortion and adjust a plug-in chi-square sibling p-value when internal
support exists. The selected-tail reference law remains unvalidated. A top-down traversal then turns the
supported annotations into terminal clusters. This manuscript records the
implemented method and the validation gaps that must be closed before
submission-level calibration or power claims can be made.
\end{abstract}

\input{sections/introduction/section}
\input{sections/method/section}
\input{sections/implementation/section}
\input{sections/experiments/section}
\input{sections/discussion/section}

\appendix
\input{sections/appendix/projected_wald}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
